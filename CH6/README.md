#javascript 표현식 기본
기본적으로 자바스크립트 일반 표현식 사용이 가능.

#javascript 표현식 삼항연산자
데이터 바인딩 영역(이중중괄호) 안에서는 조건문 등은 사용 불가.
변수 선언같은 구문도 사용이 불가능하다.

#javascript 함수
자바스크립트 객체에 정의되어 있는 함수들은 사용 가능.

#javascript 속성 바인딩
v-bind 태그의 속성 영역에도 표현식을 사용할 수 있음.
key 값으로 된 id, class 등 동적인 선택자 생성에도 사용 가능.