#v-if
v-if는 바인딩된 값에 따라서 엘리먼트를 선택적으로 표시가능.

#v-bind
바인딩된 엘리먼트의 속성 값을 동적으로 제어하는데 주로 사용.
v-if와 함께 자주 사용되는 디렉티브.

#v-on:click
인스턴스에 바인딩된 함수에 이벤트핸들링을 가능하게 해준다.
@:click 처럼 약어로 사용되는 경우가 많음.

#v-on
자바스크립트의 함수를 즉시 바인딩할 수 있음.
